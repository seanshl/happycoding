package com.Algorithms.RecursionAndDynamicProgramming;

/**
 * Write an algo to pring all ways of arranging eight queens on an 8 * 8 chess board
 * so that none of them share the same row, column or iagonal. In this case, diagnal means
 * all diagonals, not just the two that bisect the board.
 * @author liushiyao
 *
 */
public class EightQueens {

}
